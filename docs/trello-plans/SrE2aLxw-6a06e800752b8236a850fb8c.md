# Рефакторинг

Source: Trello card SrE2aLxw (id: 6a06e800752b8236a850fb8c)

## Description

Цель:
Произвести рефакторинг

Описание:
Вынести JavaScript из общего index.html; импортируемые библиотеки должны быть добавлены в dependencies в package.json. Файлы должны быть небольшими модулями, разбитыми на функции.

Примечания:
Должно быть сделано после "Добавление vite". Проект должен билдиться на vite без ошибок командой npm run build.

## Acceptance criteria

- Все inline-скрипты вынесены в модули в src/ или подходящую папку.
- package.json содержит необходимые зависимости и скрипты (dev/build).
- Проект успешно собирается командой npm run build (vite) без ошибок.
- Код разбит на понятные модули с небольшими функциями и минимальными глобальными переменными.

## Implementation steps

1. Add Vite setup (task: "Добавление vite") and ensure build passes.
2. Create src/ and move inline scripts into module files (e.g., src/game.js, src/input.js, src/render.js).
3. Replace script tags in index.html with module imports (type="module") or use bundler entry.
4. Update package.json dependencies and add scripts: dev, build, preview.
5. Run linting/build and fix issues; add basic unit/playground checks if applicable.
6. Create a PR with scoped refactor changes and manual QA steps.

## Todos

- [ ] Create src/ structure and move scripts
- [ ] Update package.json and add vite scripts
- [ ] Ensure build passes: npm run build

## Notes

- Dependent on: wzGTzoTA (Добавление vite)
- Generated by assistant using .copilot/trello-config.yml
