# Cartographer action failed

Source: Trello card NrPsXdBu (id: 6a070c81f99adef71ecef75e)

## Description

Цель:
Исправить ошибку в workflow cartographer ("No jobs were run").

Описание:
GitHub Actions workflow .github/workflows/cartographer.yml не запускает jobs; нужно восстановить процесс генерации project-map при изменении кода.

## Acceptance criteria

- Workflow .github/workflows/cartographer.yml содержит корректный `on:` триггер (push, pull_request) и по крайней мере один job, который запускается.
- Job выполняет генерацию project-map и сохраняет/коммитит обновлённый docs/project-map.html (или json) обратно в репозиторий, либо создает артефакт/PR с результатом.
- Локальная проверка: workflow запускается и успешно выполняет шаг генерации (можно с повторным запуском или через test runner).

## Implementation steps

1. Открыть файл .github/workflows/cartographer.yml и проверить блок `on:` — убедиться, что он реагирует на push/pull_request либо на нужные пути (например 'src/**', 'tools/**').
2. Проверить, что в workflows определены jobs с корректными step'ами; если jobs отсутствуют или находятся в неправильном отступе, исправить форматирование YAML.
3. Убедиться, что запускается шаг, который устанавливает зависимости и запускает инструмент cartographer (например: node tools/cartographer.js) или вызывает action, и что результат сохраняется в docs/project-map.html.
4. Добавить шаг, который коммитит/пушит сгенерённый файл обратно (если нужен) или создаёт PR/артефакт. Учесть права и токены (GITHUB_TOKEN).
5. Протестировать workflow: либо пушем в feature-ветку и запускаем, либо используем rerun/act.
6. Если workflow корректен, добавить короткий тест/monitor (schedule) и обновить README или docs/project-map.md с инструкцией на CI.

## Todos

- [ ] Inspect .github/workflows/cartographer.yml and fix triggers
- [ ] Ensure a job runs cartographer and outputs docs/project-map.html
- [ ] Add commit step or artifact creation using GITHUB_TOKEN
- [ ] Test workflow run and confirm project-map updates

## Notes

- Generated by assistant during Trello sync. Link: https://trello.com/c/NrPsXdBu
